{
	"name": "root",
	"private": true,
	"author": "codebam",
	"license": "Apache-2.0",
	"scripts": {
		"build": "lerna run build",
		"build:worker": "lerna run build --scope=worker",
		"build:rust": "lerna run build --scope=rust",
		"release": "lerna version minor -y",
		"publish": "lerna publish -y from-git",
		"lint": "lerna run lint"
	},
	"workspaces": [
		"packages/*"
	]
}
